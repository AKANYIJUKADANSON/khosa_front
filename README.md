## Installation
1. Download the files from github

2. Install all the packages
    ```bash
        npm install
    ```

3. Configure your backend url in the .env file
    e.g. VITE_API_URL=http://localhost/khosaleague.go.ug

4. Run the app
    ```bash
        npm run dev
    ```